#pragma once
#include <iostream>  // Needed to use I/O functions
#include <limits>   // integer limits
#include <iomanip>   // needed for formatting floating-point numbers
#include <string>    // use string class
using namespace std;

// (Function Declarations)(Function Headers) (Interface)

void formattedOutput();    // Formatting Output
void formattedInput();     //
void sizeofTypes();        // print Size of Fundamental Types
void integerlimits();      // print integer limits in <climits> header
void overflow();           // rithmetic Overflow/Underflow
void mixTypesOperations(); // mix-type arithmetic operations
void incrementDecrement(); // increment (++) and decrement (--) Operators
void sumNumbers();         // Counter-Controlled Loop
void sumOddEven();         // Sum the odd and even numbers (Loop)
void statNumbers();        // Sentinel-Controlled Loop
void nonPrimeList();       // break example 
void continueExe();        // continue example 
void mystery();            // Example (break and continue)